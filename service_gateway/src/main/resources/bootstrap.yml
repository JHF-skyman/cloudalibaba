server:
  port: 9099

spring:
  application:
    name: gateway-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848       #nacos中心地址
        file-extension: yaml              # 配置文件格式
